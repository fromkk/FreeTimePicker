# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Core' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Core

  target 'CoreTests' do
    # Pods for testing
  end

end

target 'FreeTimePicker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FreeTimePicker

  # add the Firebase pod for Google Analytics
  pod 'FirebaseCore'
  pod 'Firebase/Analytics'
  pod 'Fabric'
  pod 'Crashlytics'
  # add pods for any other desired Firebase products
  # https://firebase.google.com/docs/ios/setup#available-pods
  pod 'SwiftFormat/CLI'
  pod 'SwiftLint'

  target 'FreeTimePickerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FreeTimePickerUITests' do
    # Pods for testing
  end

end

target 'Siri' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Siri

end

target 'UI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for UI

end
